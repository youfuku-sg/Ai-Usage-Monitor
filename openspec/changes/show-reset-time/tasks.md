## 事前確認

- [ ] 0.1 `add-service-label` が完了済みであることを確認する

## 1. 時刻フォーマット変更

- [ ] 1.1 `src/poller.rs` の `format_line()` でリセット時刻を HH:MM 形式にフォーマットする処理を実装する
- [ ] 1.2 リセット時刻（UTC タイムスタンプ）をローカル時刻に変換する処理を確認・追加する

## 2. 表示箇所の更新

- [ ] 2.1 `src/window.rs` のウィジェット表示を `XX% · HH:MM` 形式に変更する
- [ ] 2.2 `src/window.rs` のツールチップを HH:MM 形式に変更する
- [ ] 2.3 通知メッセージ（`src/` 内の通知処理）を HH:MM 形式に変更する

## 3. 動作確認

- [ ] 3.1 `cargo build` が通ることを確認する
- [ ] 3.2 ウィジェットに `claude: 40% · 15:30` のように実時刻で表示されることを確認する
- [ ] 3.3 日付をまたぐ場合（23:50 のリセット等）に正しく表示されることを確認する
