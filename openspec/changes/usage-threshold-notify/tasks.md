## 1. 設定ファイルの対応

- [ ] 1.1 `notify_thresholds` フィールドを設定の読み書き処理に追加する（デフォルト: `[80, 90, 95, 98]`）

## 2. 状態管理の追加

- [ ] 2.1 `src/window.rs` の `AppState` に `notified_thresholds: Vec<u8>` を追加する
- [ ] 2.2 初期値を空配列（`vec![]`）で設定する

## 3. 閾値チェック処理の実装

- [ ] 3.1 ポーリング完了ハンドラ（`WM_APP_POLL_COMPLETE`）に閾値チェック処理を追加する
- [ ] 3.2 現在の使用率を設定の閾値リストと比較し、未通知の閾値を検出する
- [ ] 3.3 閾値超過時に `tray_icon::notify_balloon()` でトースト通知を送出する
- [ ] 3.4 通知済み閾値を `notified_thresholds` に追加する

## 4. リセット検出とクリア処理

- [ ] 4.1 5h ウィンドウのリセットを検出したとき（`is_past_reset` が true → false に遷移したとき）に `notified_thresholds` をクリアする

## 5. ローカライズ対応

- [ ] 5.1 `src/localization/mod.rs` の `Strings` 構造体に通知用フィールドを追加する
- [ ] 5.2 全言語ファイル（8言語）に通知用文字列を追加する

## 6. 動作確認

- [ ] 6.1 `cargo build` が通ることを確認する
- [ ] 6.2 使用率が閾値を超えたときにトースト通知が出ることを確認する
- [ ] 6.3 同じ閾値で2回目の通知が出ないことを確認する
- [ ] 6.4 ウィンドウリセット後に再び通知が出ることを確認する
- [ ] 6.5 `notify_thresholds: []` で通知が出ないことを確認する
