# Design: claude-single-line

## 変更内容

`remove-7d-status` 完了後の状態確認・レイアウト調整のみ。

### 確認ポイント

- `src/window.rs` のウィジェット高さ計算が 1 行分になっているか
- 上下の余白・パディングに余分なスペースが残っていないか

### 修正が必要な場合

`src/window.rs` の描画領域サイズ算出ロジックを、サービス行数 × 行高さ で動的に決まるよう修正する。
