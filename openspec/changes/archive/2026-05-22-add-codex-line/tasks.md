## 事前確認

- [x] 0.1 `show-reset-time` が完了済みであることを確認する

## 1. Codex データ取得

- [x] 1.1 Codex の認証情報（`$CODEX_HOME/auth.json` または `~/.codex/auth.json`）の読み取り処理を確認する
- [x] 1.2 Codex の使用量 API エンドポイントから数時間ウィンドウの使用率・リセット時刻を取得する処理を実装する
- [x] 1.3 Codex にアクセスできない場合（未契約・認証なし・API エラー）を判定する処理を実装する

## 2. データモデルの更新

- [x] 2.1 `src/models.rs` に Codex 用の使用量データ構造を追加する

## 3. ウィジェット表示の追加

- [x] 3.1 `src/window.rs` の 2 行目に `codex: XX% · HH:MM` を追加する
- [x] 3.2 Codex にアクセスできない場合は 2 行目を非表示にする
- [x] 3.3 ウィジェットの高さを Codex 表示有無に応じて動的に変える

## 4. ローカライズ対応

- [x] 4.1 `src/localization/*.rs` に `codex_label: "codex:"` を追加する（全言語）

## 5. 動作確認

- [x] 5.1 `cargo build` が通ることを確認する
- [x] 5.2 Codex 契約あり環境で 2 行目に `codex: XX% · HH:MM` が表示されることを確認する
- [x] 5.3 Codex にアクセスできない環境で 2 行目が非表示になることを確認する
